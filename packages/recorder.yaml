recorder:
  db_url: !secret postgres_url
  purge_keep_days: 7
  exclude:
    domains:
      - automation
      - weblink
      - updater
    entities:
      - sun.sun 
      - sensor.last_boot 
      - sensor.date
